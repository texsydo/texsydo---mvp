# TSD Web MVP Manual Testing

This module is meant to simulate the root of a textual system (e.g., a blog), so
you can run `tsd-web---mvp` in this directory to test and document the
integration of the tool as a final CLI application.
